import React, { PureComponent } from "react";

export default class ProgressiveImg extends PureComponent {
  render = ({
    placeholder,
    src,
    srcset,
    sizes,
    alt,
    loadStrategy,
    intersectionMargin
  }) => {
    return null;
  };
}
